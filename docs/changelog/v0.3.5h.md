# v0.3.5h: Startup Integration

**Date:** 2026-01-15

## Summary

Welcome screen and session state infrastructure for workspace restoration.

## New Files

### Core Models
- `WorkspaceState.cs` - Session state with open files, expanded folders
- `OpenFileState.cs` - Per-file state with caret and scroll position

### ViewModels
- `WelcomeViewModel.cs` - Welcome screen with recent workspaces, commands

### Views
- `WelcomeView.axaml` - Welcome screen UI with logo, buttons, tips
- `WelcomeView.axaml.cs` - Code-behind

### Tests
- `WelcomeViewModelTests.cs` - 6 unit tests

## Modified Files

### MainWindowViewModel.cs
- Added `ShowWelcome` computed property (inverse of HasOpenWorkspace)
- Added `[NotifyPropertyChangedFor(nameof(ShowWelcome))]` attribute

### MainWindow.axaml
- Added `<views:WelcomeView IsVisible="{Binding ShowWelcome}" />`

### ServiceCollectionExtensions.cs
- Registered `WelcomeViewModel` as transient

## Welcome Screen Features
- App logo and title
- "Open Folder" and "New File" buttons
- Recent workspaces list (up to 5)
- Keyboard shortcut hints (Ctrl+O, Ctrl+P, Ctrl+,)

## Verification
- ✅ Build succeeded
- ✅ Unit tests created
